# Schedule the function to run nightly at 1:00 AM UTC
# Cron format: minute hour day month day-of-week
- name: "nightly-email-import"
  schedule: "0 1 * * *"  # Every day at 1:00 AM UTC
  function: "nightly-email-import"